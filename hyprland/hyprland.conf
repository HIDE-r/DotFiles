################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# monitor=,preferred,auto,auto
# monitor=,highres,auto,1
monitor=eDP-1,highres,auto,1, mirror,HDMI-A-1
monitor=HDMI-A-1,highres,auto,1
# monitor=HDMI-A-1,1024x768,auto,1

xwayland {
    force_zero_scaling = true
}

# /run/user/1000/hypr/94c20a186372aace78b188842848b873eb3ebbd7_1715825812_1548791430/hyprland.log
debug {
    disable_logs = true
}

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 5
    gaps_out = 5

    border_size = 2

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 10
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696

        new_optimizations = on
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default curves, see https://wiki.hypr.land/Configuring/Animations/#curves
    #        NAME,           X0,   Y0,   X1,   Y1
    bezier = easeOutQuint,   0.23, 1,    0.32, 1
    bezier = easeInOutCubic, 0.65, 0.05, 0.36, 1
    bezier = linear,         0,    0,    1,    1
    bezier = almostLinear,   0.5,  0.5,  0.75, 1
    bezier = quick,          0.15, 0,    0.1,  1

    # Default animations, see https://wiki.hypr.land/Configuring/Animations/
    #           NAME,          ONOFF, SPEED, CURVE,        [STYLE]
    animation = global,        1,     10,    default
    animation = border,        1,     5.39,  easeOutQuint
    animation = windows,       1,     4.79,  easeOutQuint
    animation = windowsIn,     1,     4.1,   easeOutQuint, popin 87%
    animation = windowsOut,    1,     1.49,  linear,       popin 87%
    animation = fadeIn,        1,     1.73,  almostLinear
    animation = fadeOut,       1,     1.46,  almostLinear
    animation = fade,          1,     3.03,  quick
    animation = layers,        1,     3.81,  easeOutQuint
    animation = layersIn,      1,     4,     easeOutQuint, fade
    animation = layersOut,     1,     1.5,   linear,       fade
    animation = fadeLayersIn,  1,     1.79,  almostLinear
    animation = fadeLayersOut, 1,     1.39,  almostLinear
    animation = workspaces,    1,     1.94,  almostLinear, fade
    animation = workspacesIn,  1,     1.21,  almostLinear, fade
    animation = workspacesOut, 1,     1.94,  almostLinear, fade
    animation = zoomFactor,    1,     7,     quick
}

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

binds {
	allow_workspace_cycles = yes
}

ecosystem {
	no_update_news = true
}

misc {
	force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
	disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(

	mouse_move_enables_dpms = false
	key_press_enables_dpms =false
}

#############
### INPUT ###
#############

input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }

}

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

windowrule {
# Ignore maximize requests from all apps. You'll probably like this.
	name = suppress-maximize-events
	match:class = .*

	suppress_event = maximize
}

windowrule {
	name = hypridle
	match:class = .*

	idle_inhibit = fullscreen
}

windowrule {
# Fix some dragging issues with XWayland
	name = fix-xwayland-drags
	match:class = ^$
	match:title = ^$
	match:xwayland = true
	match:float = true
	match:fullscreen = false
	match:pin = false

	no_focus = true
}

windowrule {
	name = copyq
	match:class = .*copyq$

	float = on
	center = true
	size = (monitor_w*0.5) (monitor_h*0.5)
}


windowrule {
	name = pot
	match:class = pot

	float = on
	center = true
	size = (monitor_w*0.5) (monitor_h*0.5)
}

windowrule {
	name = goldendict
	match:class = .*GoldenDict-ng$

	float = on
	center = true
	size = (monitor_w*0.5) (monitor_h*0.5)
}

windowrule {
	name = keycombiner
	match:class = .*keycombiner$

	float = on
	center = true
	size = (monitor_w*0.5) (monitor_h*0.5)
}

windowrule {
	name = nemo
	match:class = nemo

	float = on
	center = true
	size = (monitor_w*0.5) (monitor_h*0.5)
}

windowrule {
	name = kcalc
	match:class = .*kcalc

	float = on
	center = true
	size = (monitor_w*0.5) (monitor_h*0.5)
}

windowrule {
	name = authentication-agent
	match:class = org.kde.polkit-kde-authentication-agent-1

	float = on
	center = true
	size = (monitor_w*0.5) (monitor_h*0.5)
}

windowrule {
	name = xdg-desktop-portal-gtk
	match:class = xdg-desktop-portal-gtk

	float = on
	center = true
	size = (monitor_w*0.5) (monitor_h*0.5)
}

windowrule {
	name = obsidian
	match:class = obsidian

	workspace = 8
}

windowrule {
	name = peek
	match:class = Peek

	no_blur = on
}

windowrule {
	name = drawpen
	match:class = DrawPen

	no_blur = on
}


# https://github.com/hyprwm/Hyprland/issues/6140#issuecomment-2323146497
windowrule {
	name = looking-glass
	match:class = looking-glass-client

	render_unfocused = on
}

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# https://github.com/xkbcommon/libxkbcommon/blob/master/include/xkbcommon/xkbcommon-keysyms.h
bind = $mainMod, Return, exec, wezterm start
bind = $mainMod SHIFT, C, killactive, 
bind = $mainMod SHIFT, Q, exit, 
# bind = $mainMod SHIFT, S, exec, grim -g "$(slurp)" - | swappy -f -
bind = $mainMod SHIFT, S, exec, env XDG_CURRENT_DESKTOP=Sway flameshot gui
bind = $mainMod, E, exec, nemo
bind = $mainMod, S, exec, rofi -show drun
bind = $mainMod, W, exec, rofi -show window
bind = $mainMod SHIFT, P, exec, copyq show 
bind = $mainMod SHIFT, V, exec, copyq edit -1
bind = CTRL ALT, T, exec, copyq clipboard | xargs goldendict
bind = CTRL ALT SHIFT, T, exec, curl http://127.0.0.1:60828/translate
bind = $mainMod ALT, C, exec, wezterm --config enable_wayland=false start xdotool key super+alt+c
bind = $mainMod, O, pseudo, # dwindle
bind = $mainMod SHIFT, F, fullscreen, 0
bind = $mainMod, F, fullscreen, 1
bind = $mainMod SHIFT, space, togglefloating
bind = $mainMod, P, pin, #pins a window , floating only
bind = $mainMod, R, togglesplit,
bind = $mainMod SHIFT, L, exec, hyprlock
# bind = $mainMod, B, exec, killall -SIGUSR1 waybar
bind = $mainMod, B, exec, hyprpanel toggleWindow bar-0
bind = $mainMod, I, exec, dunstctl history-pop

# Passthrough key to remote desktop
bind = CTRL , Alt_R, submap, passthrough
submap = passthrough
bind = CTRL , Alt_R, submap, reset
submap = reset

# Fn keys
# bind = , XF86TouchpadOn, exec, /home/collin/ScriptTools/Hyprland/toggle_touchpad.sh
# bind = , XF86TouchpadOff, exec, /home/collin/ScriptTools/Hyprland/toggle_touchpad.sh
bind = , XF86Calculator, exec, kcalc
bind = , XF86MonBrightnessUp, exec, light -A 5
bind = , XF86MonBrightnessDown, exec, light -U 5
bind = , XF86AudioRaiseVolume, exec, pamixer -i 5
bind = , XF86AudioLowerVolume, exec, pamixer -d 5
bind = , XF86AudioMute, exec, pamixer -t

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Move window with mainMod + ctrl + arrow keys
bind = $mainMod CTRL, H, movewindow, l
bind = $mainMod CTRL, L, movewindow, r
bind = $mainMod CTRL, K, movewindow, u
bind = $mainMod CTRL, J, movewindow, d

# Switch active workspaces with [← →]
bind = $mainMod, left, workspace, e-1
bind = $mainMod, right, workspace, e+1

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, tab, workspace, previous
# bind = $mainMod, 0, workspace, 10

$key = tab
bind = alt, $key, exec, hyprswitch gui --mod-key alt_l --key $key --close mod-key-release && hyprswitch dispatch
bind = alt shift, $key, exec, hyprswitch gui --mod-key alt_l --key $key --close mod-key-release && hyprswitch dispatch -r

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, grave, togglespecialworkspace, magic
bind = $mainMod SHIFT, grave, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# NOTE: 目前 xwayland 的窗口可以正常触发全局快捷键, 但是 wayland 窗口无法触发, 还是个不可用状态

# bind = CTRL, i, pass, ^(copyq)$
# bind = ALT, T, pass, .*Microsoft-edge$
# bind = ,F10,pass,^(com\.obsproject\.Studio)$
# bind = CTRL,T,pass,^(org\.wezfurlong\.wezterm)$
# bind = $mainMod ALT, C, sendshortcut, keycombiner

env = HYPRLAND_LOG_WLR,1

# Nvidia
# env = WLR_NO_HARDWARE_CURSORS,1

# Toolkit Backend Variables
env = GDK_BACKEND,wayland,x11
env = QT_QPA_PLATFORM,wayland;xcb
env = SDL_VIDEODRIVER,wayland
env = CLUTTER_BACKEND,wayland

# XDG Specifications
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_TYPE,wayland
env = XDG_SESSION_DESKTOP,Hyprland

# Qt Variables
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_QPA_PLATFORM,wayland;xcb
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_QPA_PLATFORMTHEME,qt5ct

# env = GTK_IM_MODULE,fcitx
env = QT_IM_MODULE,fcitx
env = XMODIFIERS,@im=fcitx
env = SDL_IM_MODULE,fcitx
env = GLFW_IM_MODULE,ibus

env = ELECTRON_OZONE_PLATFORM_HINT,wayland

#################
### AUTOSTART ###
#################

exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# exec-once=hyprpaper &
exec-once=variety &
exec-once=hyprctl setcursor Vimix-cursors 24
exec-once=xsetroot -xcf /usr/share/icons/Vimix-cursors/cursors/left_ptr 24

exec-once=/usr/lib/polkit-kde-authentication-agent-1
exec-once=hyprpanel
# exec-once=waybar
# exec-once=dunst -conf ~/.config/dunst/dunstrc

exec-once=prime-offload; sleep 1; systemctl --user start app-io.optimus_manager.OptimusManagerQt@autostart.service
exec-once=goldendict
exec-once=pot &
exec-once=blueman-applet
exec-once=copyq
exec-once=udiskie &
#exec-once=/usr/lib/systemd/systemd-networkd-wait-online -i eno1 -i wlan0 --any; obsidian &
exec-once=obsidian &
exec-once=wezterm start
exec-once=keycombiner --hidden
exec-once=~/.local/bin/sync_fcitx5.sh
exec-once=~/.local/bin/server_notify -l
exec-once=hyprswitch init --show-title &
# exec-once=/home/collin/.local/bin/clipsync.py
exec-once=hypridle
# exec-once=hyprpm reload -n

# exec-once=/home/collin/ScriptTools/Hyprland/toggle_touchpad.sh
# exec-once=espanso service start --unmanaged
# exec-once=sleep1; nwg-dock-hyprland -p "bottom" -d -nolauncher &

# exec-once = echo "Xft.dpi: 130" | xrdb -merge
# exec-once=xprop -root -f _XWAYLAND_GLOBAL_OUTPUT_SCALE 32c -set _XWAYLAND_GLOBAL_OUTPUT_SCALE 1
env = GDK_SCALE,1
env = XCURSOR_SIZE,24
env = XCURSOR_THEME,Vimix-cursors
